% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{data_validate}
\alias{data_validate}
\title{Validate an uploaded receipts data set}
\usage{
data_validate(df)
}
\arguments{
\item{df}{A data frame (or tibble) to validate.}
}
\value{
A list with:
\describe{
\item{required_cols}{The full set of required column names.}
\item{missing_cols}{Required columns absent from \code{df}.}
\item{na_counts}{Named vector of \code{NA} counts per column, or \code{NULL}
if columns are missing.}
\item{price_error}{Logical vector flagging rows where \code{Price <= 0},
or \code{NULL} if columns are missing.}
\item{amount_error}{Logical vector flagging rows where \code{Amount <= 0},
or \code{NULL} if columns are missing.}
}
}
\description{
Checks a data frame against the expected receipts schema: presence of
the required columns, counts of missing values per column, and rows
where \code{Price} or \code{Amount} are not strictly positive.
}
\details{
Expected input columns:
\itemize{
\item \code{Id} (character)
\item \code{Store} (character)
\item \code{Item} (character)
\item \code{Price} (numeric, > 0)
\item \code{Amount} (numeric, > 0)
}
}
\examples{
df <- data.frame(
  Id = "1", Store = "Store1", Item = "Bread",
  Price = 5, Amount = 1
)
data_validate(df)
}
